# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "hunspell-rs"
version = "0.5.0"
authors = [
    "Lipka Boldizsár <lipkab@zoho.com>",
    "Bernhard Schuster <bernhard@ahoi.io>",
    "Frank Schuurmans <laranja@riseup.net",
]
description = "Spellchecking with the Hunspell library"
readme = "README.md"
keywords = [
    "spellcheck",
    "hunspell",
]
license = "Apache-2.0"
repository = "https://github.com/drahnr/hunspell-rs"
resolver = "2"

[dependencies.hunspell-sys]
version = "0.3.0"
default-features = false

[dependencies.serde]
version = "^1.0"
features = ["derive"]
optional = true

[features]
bundled = ["hunspell-sys/bundled"]
default = ["bundled"]

[dev-dependencies.bincode]
version = "1.3.3"
